#ifndef MAIN_HPP
#define MAIN_HPP

#define VERSION "V0.0.0" // The version of the Salmon compiler
#define DEBUG 1 // If we are in debug mode
#define AARCH32_COMPILATION_TARGET 0
static unsigned char optimization_level = 0;
static unsigned char compilation_target = AARCH32_COMPILATION_TARGET;

#endif